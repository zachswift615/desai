{
  "name": "desai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter electron-app dev",
    "build": "pnpm -r build",
    "mcp:start": "node packages/mcp-server/dist/index.js",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
